# external-ddns

Discovers a region's public address, corroborates it across independent
observers, and records it as a Kubernetes `PublicAddress` resource for
[external-dns](https://github.com/kubernetes-sigs/external-dns) to publish.

Not affiliated with `kubernetes-sigs/external-dns`, despite the name.
