# Squash merges make the PR title the commit semantic-release reads.
name: PR title

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

permissions:
  pull-requests: read
  statuses: write

jobs:
  pr-title:
    uses: reidops/ci-templates/.github/workflows/pr-title.yml@v0.5.0
